// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyBb6DtM4PVZmBsdfE5u6bMocCh4JYvLXig",
    authDomain: "my-image-compressor.firebaseapp.com",
    databaseURL: "https://my-image-compressor.firebaseio.com",
    projectId: "my-image-compressor",
    storageBucket: "my-image-compressor.appspot.com",
    messagingSenderId: "50892359778",
    appId: "1:50892359778:web:ddab553938d5ba99f1693b"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
